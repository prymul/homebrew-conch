# homebrew-conch

Homebrew tap for [conch](https://github.com/prymul/conch).

```sh
brew tap prymul/conch
brew install conch
```

The formula in `Formula/conch.rb` is updated automatically by conch's
release workflow whenever a new version is published — no manual edits
needed.